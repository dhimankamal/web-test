import { Mulish } from 'next/font/google';

export const fontSans = Mulish({
  subsets: ['latin'],
  variable: '--font-sans',
});

export const fontMono = Mulish({
  subsets: ['latin'],
  variable: '--font-mono',
});
